apiVersion: v1
kind: Service
metadata:
  name: {{ include "framework-orchestrator.fullname" . }}
  labels:
    {{- include "framework-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "framework-orchestrator.selectorLabels" . | nindent 4 }}
