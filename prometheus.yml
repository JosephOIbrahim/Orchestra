# Prometheus configuration for Framework Orchestrator
# Used with docker-compose --profile monitoring

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'framework-orchestrator'
    static_configs:
      - targets: ['orchestrator:8080']
    metrics_path: /metrics
    scrape_interval: 10s
